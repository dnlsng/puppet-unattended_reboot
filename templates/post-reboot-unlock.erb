[Unit]
Description=Release the reboot lock for unattended reboot (Mannaged by puppet)
Before=cron.service

[Service]
ExecStart=/usr/bin/locksmithctl -endpoint='<%= @etcd_endpoints.join(',') %>' unlock '<%= @fqdn %>' 2>&1 | /usr/bin/logger -s -t post-reboot-unlock
